import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.herojerryyang.chantheory',
  appName: 'Chan Theory',
  webDir: 'web'
};

export default config;
